# py-planetarium-service
A Django REST Framework API that handles planetarium show catalog, thematic categorization, session scheduling across multiple domes, seat reservation system with row/seat validation, user booking management, and ticket generation with JWT authentication.
